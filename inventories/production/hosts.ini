[monitoring]
# The dedicated monitoring server — runs Prometheus, Alertmanager, Loki, Grafana
monitoring.example.com

[targets]
# All servers to monitor — node_exporter + Alloy installed here
server1.example.com
server2.example.com
server3.example.com

[postgres_hosts]
# Subset of [targets] that run PostgreSQL — postgres_exporter installed here
server1.example.com
server3.example.com

[mongo_hosts]
# Subset of [targets] that run MongoDB — mongodb_exporter installed here
server2.example.com

[all:vars]
ansible_user=ubuntu
