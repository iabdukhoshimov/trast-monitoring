---
- name: Open loki port (firewalld)
  ansible.posix.firewalld:
    port: "{{ loki_port }}/tcp"
    permanent: true
    state: enabled
    immediate: true
  when: ansible_facts.services['firewalld.service'] is defined
  ignore_errors: true

- name: Open loki port (ufw)
  community.general.ufw:
    rule: allow
    port: "{{ loki_port }}"
    proto: tcp
  when: ansible_facts.packages['ufw'] is defined
  ignore_errors: true
