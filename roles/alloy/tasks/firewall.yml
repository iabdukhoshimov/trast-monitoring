---
- name: Open alloy UI port (firewalld)
  ansible.posix.firewalld:
    port: "{{ alloy_http_port }}/tcp"
    permanent: true
    state: enabled
    immediate: true
  when: ansible_facts.services['firewalld.service'] is defined
  ignore_errors: true

- name: Open alloy UI port (ufw)
  community.general.ufw:
    rule: allow
    port: "{{ alloy_http_port }}"
    proto: tcp
  when: ansible_facts.packages['ufw'] is defined
  ignore_errors: true
